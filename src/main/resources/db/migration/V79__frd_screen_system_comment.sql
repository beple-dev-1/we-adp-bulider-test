comment on column builder.adk_builder_frd_screen.system_code is
    '이 화면이 사는 시스템 — 기획 저장소 manifest.json 의 systems[].id 그대로다. 기존 화면은 index.json 의 그 화면 system 이 정답이라 빌더가 그 값으로 덮는다(AI 가 적은 값을 믿지 않는다). ⛔ FRD 하나가 한 시스템이라고 보지 마라 — 걸치면 adk_builder_frd.system_code 는 비고 이 열만 찬다.';
