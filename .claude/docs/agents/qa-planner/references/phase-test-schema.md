# 페이즈별 테스트 계획 형식

각 개발 페이즈에 대응하는 TC 목록 형식이다.

## TC 번호 규칙
TC 번호 형식은 qa-planner 절차 §3 참조 (`TC-{과업번호}-{도메인코드}-{순번3자리}`).
도메인 코드는 `.claude/config/scope.yaml` 의 `scopes[].id` 대문자다 — 목록을 여기 두지 않는다.

## TC 항목 형식
```markdown
### TC-{번호}: {제목}

| 항목 | 내용 |
|------|------|
| 유형 | 단위 / 개발서버 검증 |
| 우선순위 | 높음 / 중간 / 낮음 |
| 전제조건 | {테스트 실행 전 필요한 상태} |
| 입력 | {테스트 입력값} |
| 기대결과 | {예상 동작 및 응답} |
| 대응 개발 항목 | phase_{N}.md의 구현 항목 |
```
