# CLAUDE.md — we-adk-builder

## 전체 지형 — 저장소 셋

```
extractor ──▶ 기획 레포 한 채 ──(사람이 push 한다)──▶ GitLab ──▶ builder 가 클론해 쓴다
                 (스킬 사본을 품고 나간다)                          ▲
                                                                    └─ 여기
```

| 저장소 | 무엇 | g2c 를 아나 |
|---|---|---|
| **`we-adk-builder-extractor`** | 사업 착수 때만 도는 클로드 코드 스킬. 운영 소스 → 표준 구조 | **안다 (원본이 거기다)** |
| **기획 레포** ×N | 기획팀 소유 · GitLab. 화면 · 요건 · 도메인 + **규격과 검사기 + 추출기 스킬 사본** | **안다 (사본으로. 그 사업 것만)** |
| **`we-adk-builder`** ← **여기** | 서버 1대 · 브라우저 접속. 산출물의 생애 관리 | 모른다 |

만드는 순서: **extractor → 기획 레포 → builder.**
추출기가 기획 레포 구조를 잡아주므로 순서가 뒤집히지 않는다. **여기가 마지막이다.**

**기획 레포가 g2c 를 아는 것은 사본으로만이다.** 원본은 추출기 저장소이고, 사본을 고쳐서 원본에 반영되는 길은 없다.
사본에는 **그 사업의 어댑터 설정만** 담긴다.

> **이 블록은 추출기와 빌더 두 저장소에 같은 내용으로 둔다. 참조가 아니라 사본이다.**
> 안 바뀌는 것(이름 · 역할 · 순서)만 담아서 어긋날 것이 없게 했다.
> 저쪽 문서의 §절을 가리키는 것은 여전히 금지다 — 그건 절 번호가 바뀌면 이쪽이 깨진다.
>
> **기획 레포에는 두지 않는다.** 거기 `CLAUDE.md` 는 기획자용 작업 지침이고(규격 · `reindex` ·
> `run.mjs` · `npm install`), 저장소 지형은 그 안에서 행동으로 바뀌는 것이 아니다.
> 그것을 `emitTree` 로 찍어내면 **코드가 만드는 세 번째 사본**이 생겨, emit 템플릿이 낡는 순간
> 아무도 안 보는 자리에서 갈린다. 사본은 손으로 둔 둘이면 족하다 — 「세 저장소」로 되돌리지 마라.

⛔ **위 블록에 산출물 개수를 다시 적지 마라 (2026-08-15).** 「산출물 12종」이 14종이 되며 갈라진 자리다.
추출기가 먼저 개수를 뺐고 우리가 늦게 따라갔다 — **그때는 어긋난 쪽이 우리였다.**
개수의 정본은 `docs/artifacts.md` 다.

## 상대 저장소와 대화 — 세션 메시지

두 저장소의 클로드 세션이 **사람을 거치지 않고** 직접 메시지를 주고받는다. 같은 기계에 상대 세션이
살아 있으면(`ListAgents` 로 보인다) `SendMessage` 로 묻고 답한다. 상대가 적은 것은 그대로 믿지 말고
**클론·상대 레포를 열어 실측으로 확인한다.**

- **대화는 어디에도 파일로 남지 않는다.** 그래서 **닫힌 건의 결론은 각자 자기 레포에 남긴다**
  (`docs/…md` · `HANDOFF.md`). 이 규칙을 빼면 결론이 세션과 함께 사라진다.
- 종전의 우편함 스킬(`/mailbox` · 부모 폴더의 `MAILBOX.md`)은 **2026-09-02 에 접었다**(병주 결정).
  `MAILBOX.md` 에 남은 열린 건은 처리한 뒤 파일을 지운다.

## 이 저장소가 뭔가

**서버 1대 · 브라우저 접속.** 기획자가 FRD별 전용 워크트리에서 일한다.
기획 레포(GitLab · 기획팀 소유)를 클론해 놓고, **현재 산출물의 생애를 관리한다** (개수와 목록의 정본은 `docs/artifacts.md`).

**100% 신규다.** v1 에서 옮겨오는 코드가 없다.

## 경계 — 어기면 재구성이 무의미해진다

- **g2c 를 모른다.** 소스 시스템(Thymeleaf · `dino-*`)을 읽는 코드를 여기 두지 않는다.
  추출은 사람이 클로드 코드 세션에서 `we-adk-builder-extractor` 스킬로 돌린다. 빌더가 돌리지 않는다.
- ⛔ **기관 스킨은 렌더 시점에 빌더가 갈아끼우되, 폴더 이름(`iks`·`tnj` 꼴)을 코드에 두지 않는다.**
  어느 기관이 어느 css 폴더인지는 소스가 안 적어 둔 **사업 지식**이고, 정본은 클론의
  `manifest.json` → `systems[].skins` 하나다. 그 글자를 코드가 쥐는 순간 빌더가 g2c 를 아는 것이 된다.
  설계는 `docs/superpowers/specs/2026-08-22-preview-skin-design.md`.
- 기획 레포는 **기획팀 소유**다. 여기엔 클론만 온다. 규격과 검사기는 기획 레포 안에 있다.
- **다른 저장소의 §절을 참조하지 않는다.** 필요하면 베껴 적는다. 참조가 종전 15문서를 6,695줄로 키웠다.

## 지금 단계

**단계 3.** 앞의 둘(`extractor` → 기획 레포)이 서야 시작한다.
끝 조건 = **기획자가 브라우저로 화면 하나를 고친다.**

### 현재 제품 흐름은 FRD에서 시작한다

**2026-08-20 개정.** 받은 문서·요구사항·요구사항정의서·BRD는 현재 Builder 흐름에서 제거됐다.

```
FRD 작업하기 ─┬ 걸음1 요구사항 직접 입력
              ├ 걸음2 AI 인터뷰          → FRD 작업대 → 개발요청서(DR)
              └ 걸음3 개발 범위 확인
```

⚠ **앞의 셋은 나란한 단계가 아니라 「FRD 작업하기」 마법사의 속이다 (2026-08-27 실측).**
정본은 `FrdWizardController` 와 `templates/artifacts/frd-wizard.html` 의 진행 표시 셋이고,
상태로는 `ANALYZING`·`WAITING_ANSWER`·`PICKED`(걸음2) → `SCOPE_REVIEW`(걸음3) → `DRAFTING`(작업대)이다.
⛔ **다섯을 한 줄로 나란히 그리지 마라** — 종전 도식이 그래서 「앞의 셋이 없어졌다」로 읽혔다.
왼쪽 메뉴에 그 셋이 없는 것은 사라져서가 아니라 마법사 안에 있기 때문이다.

- **작업 단위는 FRD다.** FRD 1개 = 작업 1개 = 워크트리 1개 = 한 사람.
- 워크트리는 `FRD 작업하기`를 누를 때 만들고, 생성 성공 후에만 `DRAFTING`으로 바꾼다
  (→ `docs/frd-worktree-lifecycle-design.md`).
- ⛔ **「과업」을 쓰지 않는다.** 일반 명사는 「작업」이다.
  **예외 셋** — ① 「종전에는 과업이…」처럼 **과거를 설명하는 자리** ② **밖에서 받는 문서 이름**
  (「과업요청서」) ③ **남의 규격에 적힌 말을 그대로 인용해 따지는 자리**
  (추출기 규격의 `과업ID`·「과업 축」 — 우리가 이름을 바꿔 적으면 그쪽과 대조가 안 된다).
  폐어 넷에 이미 ①의 예외가 있다
- **코드 식별자는 영문, 설명하는 글은 한글이다** — 정본은 `docs/coding-conventions.md`.
  `data-model` §0 이 DB 에 정한 「열은 영문 · 뜻은 한글 COMMENT」를 자바와 화면에 그대로 편 것이다.
  ⚠ **테스트 메서드 이름은 한글이 규칙이다** — 호출되는 이름이 아니라 무엇을 보장하나를 적는 문장이다
- **메뉴구조도(IA)는 AI의 화면 판단 자료다** — 낡으면 FRD가 엉뚱한 화면을 지목한다
- **그린존은 2026-08-27 에 열렸다 (병주 확정).** 그린존은 `기능명세서` · `화면설계서` · `단위테스트` · `통합테스트` · `사용자 매뉴얼` **다섯**이고 왼쪽 메뉴의 초록 구역이다. **요구사항 추적 매트릭스는 같은 날 삭제됐다** — 되살리지 마라. 착수 순서는 기능명세서(화면 md 보기 창) → 사용자 매뉴얼 → 화면설계서이고, 단위·통합테스트 화면은 **개발 완료분 역류 수신이 선 뒤**다. 기능명세서는 새 산출물이 아니라 as-is 화면 md 를 보여 주는 자리다(2026-08-25 흡수 결정 그대로).

### 문서 상태

**설계 문서 스물여섯이 `docs/superpowers/specs/` 에 서 있다.**
자바 주석의 `정본: docs/superpowers/specs/...` 가 그 문서를 가리킨다 — 클래스가 왜 그런지는 거기 있다.

⚠ **머리에 「⚠ …개정」이 붙은 문서가 여럿이다.** 앞단(받은 문서·요구사항정의서·BRD)을
설명하는 문단은 **이력이지 현재가 아니다.** 현재 산출물의 정본은 `docs/artifacts.md` 다.

## 작업 규칙

- **문서 하나 = 질문 하나.**
- **화면을 만들거나 고칠 땐 — 목업이든 템플릿이든 — `docs/mockup-conventions.md` 를 먼저 본다** —
  화면 구성 · **무엇으로 만드나** · UX 카피 · 접근성 · 공통 셸 검증 규칙이 거기 있다.
  ⛔ **새 CSS 클래스를 짓기 전에 그 물건이 이미 있나 찾는다** — 목업과 템플릿이 같은
  스타일시트를 함께 쓴다. 찾는 법과 지을 때의 규칙은 그 문서의 「무엇으로 만드나」에 있다.
  ⛔ **메뉴를 바꾸면 `ShellContract.java` · `fragments/parts.html` · `_shell.js` 셋을 함께 맞춘다.**
- **미결은 본문에 적지 않는다 → GitHub 이슈로.** 종전에 마커 510건(13줄마다 하나)이 문서 안에서 썩었다.
- 문서 500줄은 경보음이지 장치가 아니다. 넘으면 쪼갤 질문을 찾아라.
### 검증은 변경 위험도에 비례시킨다

**많이 돌리는 것 자체를 품질로 보지 않는다.**

- **단순 디자인 수정** — 문구·버튼 이름과 위치·간격·색상·HTML/CSS 배치만 바꾸고
  라우팅·조건 분기·화면 모델·서버 동작을 안 건드렸을 때:
  `git diff --check` + 변경 화면 데스크톱·375px 확인 + 목업을 고쳤으면 `check_mockups.py`.
  **전체 Maven 테스트와 별도 광범위 리뷰 금지.**
- **Thymeleaf 조건·폼 값·권한 노출·DOM 계약 변경** — 그 계약을 직접 다루는 테스트 클래스만 실행한다.
- **Java 동작·라우팅·DB·보안·공통 셸 변경** — 관련 테스트에서 시작하고
  영향 범위가 넓을 때만 전체(`./mvnw test`)로 확대한다.
- 기존의 무관한 실패는 이번 작업에서 쫓아가 고치지 말고 필요한 만큼만 확인해 보고한다.

⚠️ **「빨리」는 기능 범위를 좁혀서 얻고, 검증은 위 등급에 맞춘다.**
로직 변경에서 테스트를 얕게 쓰지는 않는다. TDD 는 기능·버그 수정에 그대로 적용한다.

**기능 개발·로직 변경의 끝선은 `./mvnw test` 초록이다.** 화면이 실제로 뜨는지는 사람이 본다 —
기계가 재는 것과 사람이 보는 것의 경계다.

### ⛔ 마이그레이션 번호를 문서에 박지 마라 — 파일을 만들기 직전에 `ls` 로 잰다 (2026-08-25)

```
ls src/main/resources/db/migration/ | sed 's/__.*//' | sort -V | tail -1
```

⛔ **마이그레이션을 더했으면 ERD 를 다시 그린다 — 커밋 전에.**

```
python docs/tools/erd_from_migrations.py
```

`docs/data-model.md` §2 는 **손으로 적지 않는다.** 그 스크립트가 마이그레이션 전부를
버전 순서로 재생해 표·열·관계를 다시 그린다. 낡았는지만 보려면 `--check`(낡으면 `1` 로 죽는다).
⚠ **열의 뜻은 `COMMENT ON COLUMN` 에 적어라** — ERD 의 설명이 그것을 그대로 옮기므로
DB 에 안 적으면 그림에도 안 나오고, 그 자리가 문서에 「뜻이 안 적힌 열」로 드러난다.

⛔ **이미 적용된 마이그레이션 파일은 주석 한 글자도 고치지 마라.** Flyway 가 체크섬을 재서
`Migration checksum mismatch` 로 기동을 거절한다. 고칠 것이 있으면 새 마이그레이션을 더한다.

**같은 자리를 네 번 밟았다** — `V21` · `V46` · `V50` · `V52`. 마지막 둘은 **하루 안에** 났고,
그중 하나는 **계획서에 「착수하는 순간에 다시 재라」를 적어 놓고 몇 시간 뒤에 그대로 밟은 것**이다.

⭐ **「조심하라」가 안 통하는 자리다** — 미추적 파일은 **`git log` 에도 `git status` 요약에도
워크트리에도 안 보이고 `ls` 로만 보인다.** 이름이 다르면 git 병합은 조용히 되고
**Flyway 실행 시점에 「Found more than one migration with version N」으로 컨텍스트 전체가 거절된다.**

⚠ 부딪히면 **내 것을 옮긴다 — 남의 미커밋·미추적 파일은 건드리지 않는다.**

## 사용자

기획자다. 개발자가 아니다. **구조를 바꾸는 일은 파일을 뜯기 전에 먼저 확인한다** —
"믿어줄게"를 받았어도 확인한다.

## v1 을 읽을 때

동결 저장소 `we-adk-builder-v1` 은 읽어도 된다. 다만 **참고이지 권위가 아니다.**
그 결정들은 「저장소 하나 + 로컬 설치형 도구」 전제에서 나왔고 그 전제가 폐기됐다.
**옛 결정 ID(`AR*`·`RQ*`·`IM*`·`AN*`·`D*`)에 새 설계를 맞추지 마라** — 폐기된 전제가 구조로 되살아난다.

| 무엇 | 어디 (v1 안) |
|---|---|
| 창립 문서 | `docs/superpowers/specs/2026-08-07-three-repo-rebuild-design.md` |
| 종전 설계 15문서 (동결) | `docs/design/` |
| 산출물 12종 실측 원본표 (2026-08-06) | 캡틴 원장 · `docs/design/` |

---

@CLAUDE.local.md

> 위 줄은 **개인 작업 지침**을 불러온다 — 세션 인수인계(`HANDOFF.md`) · 구현 계획 운용 ·
> 커밋 흐름처럼 **사람마다 다른 것**이 거기 있다. 저장소에 없는 파일이라 정상이다.
> 자기 것을 만들려면 루트에 `CLAUDE.local.md` 를 두면 된다 (`.gitignore` 에 이미 걸려 있다).
